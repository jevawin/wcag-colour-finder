---
phase: 04-modes-and-configuration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - MODE-01
  - MODE-03
  - CFG-01
  - CFG-02
  - CFG-03
  - MODE-02
must_haves:
  truths:
    - "Browser smoke covers: default values, live BG update, BG validation, swatch clearing on BG change, URL debounce, URL hydrate, no auto-Find"
    - "REQUIREMENTS.md marks MODE-01 and MODE-03 as DROPPED with reason"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table for Phase 4 outcomes"
      contains: "DROPPED"
  key_links:
    - from: "REQUIREMENTS.md traceability table"
      to: "CONTEXT.md D-01 (single-mode dropped)"
      via: "status column update"
      pattern: "DROPPED"
---

<objective>
Close out Phase 4: run the manual smoke checks that are not automatable (browser-only behaviours per 04-VALIDATION.md), then update REQUIREMENTS.md to reflect the dropped MODE-01/MODE-03 scope per CONTEXT.md D-01.

Purpose: The automated test suite (plans 04-01 and 04-02) cannot verify default-value rendering, debounce timing, full-page reload hydration, or DOM-mediated UX flows. A single browser smoke pass closes those gaps before the phase is signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-modes-and-configuration/04-CONTEXT.md
@.planning/phases/04-modes-and-configuration/04-VALIDATION.md
@.planning/phases/04-modes-and-configuration/04-01-SUMMARY.md
@.planning/phases/04-modes-and-configuration/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — mark MODE-01 and MODE-03 as DROPPED</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current Colour Modes section + Traceability table)
    - .planning/phases/04-modes-and-configuration/04-CONTEXT.md (D-01 — dual-only pivot)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md:

    1. In the "### Colour Modes" section, replace the three list items with:
       ```
       - [~] **MODE-01**: ~~Single-colour mode~~ — DROPPED in Phase 4 per D-01 (user pivoted to dual-only)
       - [x] **MODE-02**: Dual-colour mode — finds two close shades, one for light BG, one for dark BG, each passing AA
       - [~] **MODE-03**: ~~Toggle between modes~~ — DROPPED in Phase 4 per D-01 (no toggle; dual-only)
       ```

    2. In the "### Configuration" section, mark all three items complete:
       ```
       - [x] **CFG-01**: Inline hex inputs to set custom light background colour (default #ffffff)
       - [x] **CFG-02**: Inline hex inputs to set custom dark background colour (default #000000)
       - [x] **CFG-03**: Hex colour stored in URL for shareable links (updates on input change)
       ```

    3. In the Traceability table, update rows:
       ```
       | MODE-01 | Phase 4 | DROPPED |
       | MODE-02 | Phase 4 | Complete |
       | MODE-03 | Phase 4 | DROPPED |
       | CFG-01  | Phase 4 | Complete |
       | CFG-02  | Phase 4 | Complete |
       | CFG-03  | Phase 4 | Complete |
       ```

    4. Update the "Coverage" block:
       ```
       - v1 requirements: 22 total (+ Phase 1 engine foundation with no explicit req IDs)
       - Mapped to phases: 22 (20 active, 2 DROPPED in Phase 4)
       - Unmapped: 0
       ```

    5. Bump the "Last updated" footer to 2026-04-18 with note: "Phase 4 closeout — MODE-01/MODE-03 dropped per D-01."
  </action>
  <verify>
    <automated>node -e "const f=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8'); for(const s of ['| MODE-01 | Phase 4 | DROPPED |','| MODE-02 | Phase 4 | Complete |','| MODE-03 | Phase 4 | DROPPED |','| CFG-01  | Phase 4 | Complete |','| CFG-02  | Phase 4 | Complete |','| CFG-03  | Phase 4 | Complete |','MODE-01**: ~~Single-colour mode~~','MODE-03**: ~~Toggle between modes~~']) { if(!f.includes(s)) throw new Error('missing: '+s); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains the literal substring `| MODE-01 | Phase 4 | DROPPED |`
    - REQUIREMENTS.md contains the literal substring `| MODE-02 | Phase 4 | Complete |`
    - REQUIREMENTS.md contains the literal substring `| MODE-03 | Phase 4 | DROPPED |`
    - REQUIREMENTS.md contains the literal substring `| CFG-01  | Phase 4 | Complete |`
    - REQUIREMENTS.md contains the literal substring `| CFG-02  | Phase 4 | Complete |`
    - REQUIREMENTS.md contains the literal substring `| CFG-03  | Phase 4 | Complete |`
    - The Colour Modes section uses `~~strikethrough~~` on the dropped requirement names
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects Phase 4 actual scope: MODE-02 + all CFG complete, MODE-01/MODE-03 dropped</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser smoke — manual verifications from 04-VALIDATION.md</name>
  <read_first>
    - .planning/phases/04-modes-and-configuration/04-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/04-modes-and-configuration/04-UI-SPEC.md (Interaction Contracts)
  </read_first>
  <what-built>
    Plans 04-01 and 04-02 delivered: dual-pair findVariantPairs, url-state.js, BG inputs on each panel (defaults #ffffff and #000000), pair swatch row, debounced URL hash sync, page-load URL hydration without auto-Find, BG validation with red-border and aria-invalid pattern, BG change clears stale pair swatches.
  </what-built>
  <files>(none — manual browser verification only)</files>
  <action>This is a checkpoint task. The implementer pauses execution and follows the steps in <how-to-verify>. No file edits are performed in this task.</action>
  <verify>Manual confirmation by user (no automated command — see <how-to-verify> below).</verify>
  <how-to-verify>
    Open `index.html` in a browser (open via `file://` or run `python3 -m http.server` and visit `http://localhost:8000`). Run through each check in order:

    1. **Defaults (CFG-02):** Confirm light BG input shows `ffffff`, dark BG input shows `000000`. Inspect dark panel computed background — must be `rgb(0, 0, 0)`, NOT `rgb(17, 17, 17)`.
    2. **URL hydrate (CFG-03):** Visit `index.html#/ff0000/eeeeee/222222`. Confirm hex input shows `FF0000`, light BG input shows `eeeeee`, dark BG input shows `222222`, both panels render with those values, and NO swatch row appears (Find did not auto-run per D-15).
    3. **URL debounce (CFG-03):** Type slowly into the hex input. After ~300ms idle, confirm `location.hash` updates to `#/<typed>/<lightBg>/<darkBg>` lowercase. Confirm typing rapidly does NOT spam history (back button still goes to previous page, not previous keystroke).
    4. **Live BG update (D-12):** With a colour like #2563EB rendered, change the light BG to `cccccc`. Confirm the light panel's background AND its contrast ratio update immediately.
    5. **BG validation (D-11):** Type `xyz` into the dark BG input. Confirm red border appears, `aria-invalid="true"`, error message "Enter a valid hex colour" shows below, panels keep last valid dark BG.
    6. **BG change clears pairs (D-12):** Click "Find accessible colour" — pair swatches appear. Then change light BG to `eeeeee`. Confirm the swatch row clears (hidden again).
    7. **Pair click (D-07, D-08):** Click Find again. Click any pair. Confirm the light panel renders with the lightHex on light BG and the dark panel renders with the darkHex on dark BG. Confirm the hex input value did NOT change. Confirm the clicked pair has the selected ring (3px --user-colour outline).
    8. **No auto-Find on hash change:** Reload the page on a hash URL. Confirm panels populate but no pair swatches show.

    If all 8 pass, type `approved`. If any fail, describe which check and the observed behaviour.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms all 8 smoke checks pass with the word "approved" or equivalent
    - If any fail, the failure is captured for follow-up planning (gap closure)
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>All 8 manual smoke checks pass; phase closeout ready</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md updated and committed
- All 8 manual smoke checks pass
- Full automated suite still green: `node --test test/`
</verification>

<success_criteria>
- REQUIREMENTS.md reflects MODE-01 and MODE-03 as DROPPED, all other Phase 4 requirements as Complete
- Browser smoke confirms behaviour beyond what node:test can cover
- Phase 4 ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/04-modes-and-configuration/04-03-SUMMARY.md` recording: REQUIREMENTS.md changes, smoke check outcomes, any deferred follow-ups.
</output>
